import { ThemedText } from "@/components/libs/ThemedText";
import React from "react";
import { View } from "react-native";

const LiveSupport = () => {
  return (
    <View>
      <ThemedText>live support</ThemedText>
    </View>
  );
};

export default LiveSupport;
